@View {
  const { content } = props || {};
  return div([
    div([
      div("MARKETING LAYOUT", { className: "bg-yellow-100 text-yellow-800 text-xs font-bold px-4 py-1 text-center" }),
      content
    ])
  ]);
}